{% set name = "sqlalchemy-risingwave" %}
{% set version = "1.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 784de7f87deb39a4a7d792858591efa82aab6f6936f665d029b4b8c5c53ea1e7

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - sqlalchemy >=1.4,<2

test:
  imports:
    - sqlalchemy_risingwave
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/risingwavelabs/risingwave
  summary: RisingWave dialect for SQLAlchemy
  dev_url: https://github.com/risingwavelabs/sqlalchemy-risingwave
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - gforsyth
    - KeXiangWang
